apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: hello-bun
  namespace: monitoring
spec:
  namespaceSelector:
    matchNames:
      - default
  selector:
    matchLabels:
      app: hello-bun
  endpoints:
    - port: http
      path: /metrics
      interval: 15s
